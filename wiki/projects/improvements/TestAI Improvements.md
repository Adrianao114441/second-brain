---
title: TestAI Improvements
type: improvements
last-updated: 2026-07-06
---

# TestAI Improvements

Suggested improvements for [[TestAI]]. These are agent suggestions, not commitments; move an item into `priorities.md` when Adrian decides to act on it.

## Knowledge Gaps

- Read `Untitled.ipynb` and either name it after its contents or fold it into an existing script.
- Confirm whether the implementations use PyTorch or TensorFlow; the project page says "PyTorch or TensorFlow".

## Wiki

- The page links to concept pages that do not exist yet ([[Convolutional Neural Network]], [[Recurrent Neural Network]], [[Transformer]], [[Variational Autoencoder]], [[Q-Learning]], [[Soft Actor-Critic]]). Create them as short concept notes, since this sandbox is exactly where those concepts are being learned. A draft `Soft Actor-Critic.md` already sits at the vault root and could seed the first one.

## Next Steps

- Add a one line result comment at the top of each script (dataset, final loss or reward) so revisiting an experiment months later does not require rerunning it.
